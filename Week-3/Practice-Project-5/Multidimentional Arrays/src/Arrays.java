public class Arrays {
    private int numbers[][] = new int[3][2];
    
    Arrays(){
        // for (int i = 0; i < numbers.length; i++) {
        //     for(int j = 0; j< numbers.length)
        //     numbers[i] = i*i;
        // }

        System.out.println(numbers.length);
        // for (int i = 0; i < numbers.length; i++) {
        //     System.out.println(numbers[i]);
        // }
    }
}
